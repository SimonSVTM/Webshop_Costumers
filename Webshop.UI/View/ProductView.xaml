<UserControl x:Class="Webshop.UI.View.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Webshop.UI.ViewModel"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:ProductViewModel />
    </UserControl.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Products" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

        <WrapPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Name:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Text="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,0,10,0"/>
            <TextBlock Text="Category:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" DisplayMemberPath="Name"
                      Width="130" Margin="0,0,10,0"/>
            <TextBlock Text="Price:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}" Width="80" Margin="0,0,10,0"/>
            <TextBlock Text="Stock:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Text="{Binding StockQuantity, UpdateSourceTrigger=PropertyChanged}" Width="60"/>
        </WrapPanel>

        <WrapPanel Grid.Row="2" Margin="0,0,0,10">
            <Button Content="Add Product" Command="{Binding AddProductCommand}" Margin="0,0,6,0"/>
            <Button Content="Add to Cart" Command="{Binding AddToCartCommand}" Margin="0,0,6,0"/>
            <Button Content="Update" Command="{Binding UpdateProductCommand}" Margin="0,0,6,0"/>
            <Button Content="Delete" Command="{Binding DeleteProductCommand}" Margin="0,0,6,0"/>
            <Button Content="Clear" Command="{Binding ClearCommand}" Margin="0,0,6,0"/>
            <Button Content="Get All" Command="{Binding GetAllCommand}"/>
        </WrapPanel>

        <DataGrid Grid.Row="3" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ProductID}" Width="50"/>
                <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="130"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat={}{0:C}}" Width="90"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Margin="0,10,0,0">
            <TextBlock Text="Search" FontWeight="Bold"/>
            <WrapPanel Margin="0,5,0,0">
                <TextBox Text="{Binding SearchName, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,0,6,0"/>
                <Button Content="Get By Name" Command="{Binding GetByNameCommand}" Margin="0,0,12,0"/>
                <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SearchCategory}"
                          DisplayMemberPath="Name" Width="130" Margin="0,0,6,0"/>
                <Button Content="Get By Category" Command="{Binding GetByCategoryCommand}"/>
            </WrapPanel>
            <TextBlock Text="Shopping Cart:" FontWeight="Bold" Margin="0,8,0,2"/>
            <ItemsControl ItemsSource="{Binding ShoppingCart}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ProductName}" Margin="0,1,0,1"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>