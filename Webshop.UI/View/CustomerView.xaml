<UserControl x:Class="Webshop.UI.View.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Customers" FontSize="24" FontWeight="Bold" Margin="0,0,0,12"/>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                <TextBlock Text="First name"/>
                <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="8,0">
                <TextBlock Text="Last name"/>
                <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Column="2" Margin="8,0">
                <TextBlock Text="Email"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Column="3" Margin="8,0,0,0">
                <TextBlock Text="Points"/>
                <TextBox Text="{Binding Points, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,15,0">
                <TextBlock Text="Address"/>
                <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6"/>
                <TextBlock Text="City"/>
                <TextBox Text="{Binding City, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6"/>
                <TextBlock Text="Country"/>
                <TextBox Text="{Binding Country, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

                <WrapPanel>
                    <Button Content="Add Customer" Command="{Binding AddCustomerCommand}" Margin="0,0,6,6" Padding="10,5"/>
                    <Button Content="Update" Command="{Binding UpdateCustomerCommand}" Margin="0,0,6,6" Padding="10,5"/>
                    <Button Content="Delete" Command="{Binding DeleteCustomerCommand}" Margin="0,0,6,6" Padding="10,5"/>
                    <Button Content="Clear" Command="{Binding ClearCommand}" Margin="0,0,6,6" Padding="10,5"/>
                </WrapPanel>

                <Separator Margin="0,8,0,10"/>
                <TextBlock Text="Search" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock Text="Name"/>
                <TextBox Text="{Binding SearchName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6"/>
                <WrapPanel>
                    <Button Content="Get All" Command="{Binding GetAllCommand}" Margin="0,0,6,6" Padding="10,5"/>
                    <Button Content="Get By Name" Command="{Binding GetByNameCommand}" Margin="0,0,6,6" Padding="10,5"/>
                </WrapPanel>
                <TextBlock Text="Customer ID" Margin="0,8,0,0"/>
                <TextBox Text="{Binding SearchID, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6"/>
                <Button Content="Get By ID" Command="{Binding GetByIDCommand}" CommandParameter="{Binding SearchID}" Padding="10,5" HorizontalAlignment="Left"/>
            </StackPanel>

            <DataGrid Grid.Column="1" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding CustomerID}" Width="50"/>
                    <DataGridTextColumn Header="First name" Binding="{Binding FirstName}" Width="*"/>
                    <DataGridTextColumn Header="Last name" Binding="{Binding LastName}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.5*"/>
                    <DataGridTextColumn Header="City" Binding="{Binding City}" Width="*"/>
                    <DataGridTextColumn Header="Country" Binding="{Binding Country}" Width="*"/>
                    <DataGridTextColumn Header="Points" Binding="{Binding Points}" Width="70"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
