<UserControl x:Class="Webshop.UI.View.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Webshop.UI.ViewModel"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="600">

   

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Categories" FontSize="20" FontWeight="Bold" Margin="0,0,0,12" />

        <!-- Input fields and action buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Name:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                     Width="200" VerticalAlignment="Center" Margin="0,0,12,0" />

            <Button Content="Add" Command="{Binding AddCommand}" Width="80" Margin="0,0,8,0" />
            <Button Content="Update" Command="{Binding UpdateCommand}" Width="80" Margin="0,0,8,0" />
            <Button Content="Delete" Command="{Binding DeleteCommand}" Width="80" Margin="0,0,8,0" />
            <Button Content="Clear" Command="{Binding ClearCommand}" Width="80" />
        </StackPanel>

        <!-- List of categories -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Categories}"
                  SelectedItem="{Binding SelectedCategory}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding CategoryId}" Width="60" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>